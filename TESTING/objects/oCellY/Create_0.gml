speed = 0;
direction = 0;
ypos = y;
xpos = x;
seePosX = x
seePosY = y

waitTime = 0

returnHome = false


/*globalvar grid;
grid = mp_grid_create(0,0,room_width / 80, room_height / 80, 80, 80);
mp_grid_add_instances(grid, oWall, false);
mp_grid_add_instances(grid, oHazard, false);
mp_grid_add_instances(grid, oDmg1, false);
with (oCell)
	{
		path = path_add();
		if mp_grid_path(grid, path, x,y,oPlayer.x, oPlayer.y, 0) 
		{
			path_start(path, 3, path_action_reverse, 1);
		}
	}*/